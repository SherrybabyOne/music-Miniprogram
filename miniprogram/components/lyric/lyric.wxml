<scroll-view hidden="{{ isLyricShow }}" scroll-y="true" class="lyric-scroll">
  <view class="lyric-panel">
    <block wx:for="{{lrcList}}" wx:key="item">
      <view class="lyric">{{item.lrc}}</view>
    </block>
  </view>
</scroll-view>