<view class="player-container" style="background:url({{ picUrl }}) center/cover no-repeat" ></view>
<view class="player-mask"></view>

<view class="player-info">
  <!-- 封面信息 -->
  <view class="player-disc {{isPlaying?'play':''}}">
    <image class="player-img rotation {{isPlaying?'':'rotation-paused'}}" src="{{ picUrl }}"></image>
  </view>
  <!-- 进度条 -->
  <view class="progress-bar">
    <x-progress-bar />
  </view>
  <!-- 控制面板 -->
  <view class="control">
    <view class="iconfont icon-shangyishoushangyige" bindtap="onPrev"></view>
    <view class="iconfont {{isPlaying?'icon-zanting':'icon-bofang'}}" bindtap="togglePlaying"></view>
    <view class="iconfont icon-xiayigexiayishou" bindtap="onNext"></view>
  </view>

</view>